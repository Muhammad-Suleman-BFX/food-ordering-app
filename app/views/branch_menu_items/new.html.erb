<div class="detail-actions">
  <%= link_to "← Cancel", branch_menu_path(@branch), class: "btn btn-secondary" %>
</div>

<div class="detail-header">
  <h1>Add New Menu Item for: <%= @branch.name %></h1>
</div>

<div class="card">
  <%= render "form", available_menu_items: @available_menu_items %>
</div>
